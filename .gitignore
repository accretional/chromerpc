# Binaries
bin/
# Stray top-level build artifacts (go build ./cmd/... without -o)
/chromerpc
/recipe2json

# Temp files
temp/

# Session screenshots (contain PII: addresses, OTP/checkout pages, gift card) —
# archived locally (~/claude-amazon.zip), not committed
**/shots/

# macOS
.DS_Store
**/.DS_Store