// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.1
// - protoc             v7.34.1
// source: proto/chromeman/chromeman.proto

package chromeman

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	ChromeMan_History_FullMethodName = "/chromeman.ChromeMan/History"
)

// ChromeManClient is the client API for ChromeMan service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ChromeMan is the read-only management/monitoring surface of the bidi proxy
// (chrome-proxy). It exposes the history of the proxy's connections to remote
// chromerpc interactive sessions so an operator UI can render and follow them
// live. There is intentionally NO mutation surface in this first pass.
type ChromeManClient interface {
	// History streams the state of every connection the proxy holds. The first
	// message(s) carry each connection's existing (ring-buffered) calls; after
	// that, a message is pushed whenever a connection changes — a new call was
	// made (carried in `calls`) or its state/error changed (empty `calls`).
	//
	// Streaming is APPEND-ONLY: a client keys by connection_id, updates the
	// connection-level fields to the latest values, and APPENDS every `calls`
	// entry. Screenshot media therefore travels exactly once (when the call
	// happens), never re-sent. The stream lives for the life of the RPC — the
	// proxy forwards it into an operator websocket.
	History(ctx context.Context, in *emptypb.Empty, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ConnectionHistory], error)
}

type chromeManClient struct {
	cc grpc.ClientConnInterface
}

func NewChromeManClient(cc grpc.ClientConnInterface) ChromeManClient {
	return &chromeManClient{cc}
}

func (c *chromeManClient) History(ctx context.Context, in *emptypb.Empty, opts ...grpc.CallOption) (grpc.ServerStreamingClient[ConnectionHistory], error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	stream, err := c.cc.NewStream(ctx, &ChromeMan_ServiceDesc.Streams[0], ChromeMan_History_FullMethodName, cOpts...)
	if err != nil {
		return nil, err
	}
	x := &grpc.GenericClientStream[emptypb.Empty, ConnectionHistory]{ClientStream: stream}
	if err := x.ClientStream.SendMsg(in); err != nil {
		return nil, err
	}
	if err := x.ClientStream.CloseSend(); err != nil {
		return nil, err
	}
	return x, nil
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ChromeMan_HistoryClient = grpc.ServerStreamingClient[ConnectionHistory]

// ChromeManServer is the server API for ChromeMan service.
// All implementations must embed UnimplementedChromeManServer
// for forward compatibility.
//
// ChromeMan is the read-only management/monitoring surface of the bidi proxy
// (chrome-proxy). It exposes the history of the proxy's connections to remote
// chromerpc interactive sessions so an operator UI can render and follow them
// live. There is intentionally NO mutation surface in this first pass.
type ChromeManServer interface {
	// History streams the state of every connection the proxy holds. The first
	// message(s) carry each connection's existing (ring-buffered) calls; after
	// that, a message is pushed whenever a connection changes — a new call was
	// made (carried in `calls`) or its state/error changed (empty `calls`).
	//
	// Streaming is APPEND-ONLY: a client keys by connection_id, updates the
	// connection-level fields to the latest values, and APPENDS every `calls`
	// entry. Screenshot media therefore travels exactly once (when the call
	// happens), never re-sent. The stream lives for the life of the RPC — the
	// proxy forwards it into an operator websocket.
	History(*emptypb.Empty, grpc.ServerStreamingServer[ConnectionHistory]) error
	mustEmbedUnimplementedChromeManServer()
}

// UnimplementedChromeManServer must be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedChromeManServer struct{}

func (UnimplementedChromeManServer) History(*emptypb.Empty, grpc.ServerStreamingServer[ConnectionHistory]) error {
	return status.Error(codes.Unimplemented, "method History not implemented")
}
func (UnimplementedChromeManServer) mustEmbedUnimplementedChromeManServer() {}
func (UnimplementedChromeManServer) testEmbeddedByValue()                   {}

// UnsafeChromeManServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to ChromeManServer will
// result in compilation errors.
type UnsafeChromeManServer interface {
	mustEmbedUnimplementedChromeManServer()
}

func RegisterChromeManServer(s grpc.ServiceRegistrar, srv ChromeManServer) {
	// If the following call panics, it indicates UnimplementedChromeManServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&ChromeMan_ServiceDesc, srv)
}

func _ChromeMan_History_Handler(srv interface{}, stream grpc.ServerStream) error {
	m := new(emptypb.Empty)
	if err := stream.RecvMsg(m); err != nil {
		return err
	}
	return srv.(ChromeManServer).History(m, &grpc.GenericServerStream[emptypb.Empty, ConnectionHistory]{ServerStream: stream})
}

// This type alias is provided for backwards compatibility with existing code that references the prior non-generic stream type by name.
type ChromeMan_HistoryServer = grpc.ServerStreamingServer[ConnectionHistory]

// ChromeMan_ServiceDesc is the grpc.ServiceDesc for ChromeMan service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var ChromeMan_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "chromeman.ChromeMan",
	HandlerType: (*ChromeManServer)(nil),
	Methods:     []grpc.MethodDesc{},
	Streams: []grpc.StreamDesc{
		{
			StreamName:    "History",
			Handler:       _ChromeMan_History_Handler,
			ServerStreams: true,
		},
	},
	Metadata: "proto/chromeman/chromeman.proto",
}
